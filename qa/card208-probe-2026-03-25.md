# QA Report: Cards 208 + 209 + 206 — Full Smoke Test

**Agent:** Probe
**Date:** 2026-03-25
**Build:** builds/shoofly-advanced/install.sh (unstaged changes on main)
**Scope:** Card 208 (Advanced installer + unified alert view), Card 209 (install.sh hardening F1/F2/F4), Card 206 (hook-alerts.log session_id + agent_name attribution)

---

## 1. Card 209 — install.sh Hardening

### F1: BASE_URL pinned to tag (not main)

| File | Verdict | Detail |
|------|---------|--------|
| builds/shoofly-advanced/install.sh | **PASS** | Line 13: `BASE_URL="https://raw.githubusercontent.com/shoofly-dev/shoofly/v1.2.2"` — pinned to `v1.2.2`. |
| builds/shoofly-basic/install.sh | **FAIL** | Line 14: `BASE_URL="https://raw.githubusercontent.com/shoofly-dev/shoofly/main"` — still points to `main`. Basic build was not hardened. |

### F2: mktemp for temp files (no predictable /tmp/shoofly-* paths)

| File | Verdict | Detail |
|------|---------|--------|
| builds/shoofly-advanced/install.sh | **PASS** | Line 172: `_TMP=$(mktemp)` — proper mktemp, no predictable path. |
| advanced/install.sh (source) | **FAIL** | Lines 142–143: `jq ... > /tmp/shoofly-cfg-adv.json && mv /tmp/shoofly-cfg-adv.json ...` — predictable temp path. Build copy was fixed but source was not updated to match. |
| advanced/bin/shoofly-daemon | **WARN** | Lines 328, 622: `mktemp -u /tmp/shoofly-sock-XXXXXX` and `mktemp -u /tmp/shoofly-daemon-XXXXXX` — uses mktemp with random suffix (acceptable for FIFOs), but `-u` flag is technically unsafe (race condition between name generation and mkfifo). Low risk in practice. |
| basic/bin/shoofly-daemon | **WARN** | Line 402: Same `mktemp -u /tmp/shoofly-daemon-XXXXXX` pattern. Same note as above. |

### F4: shoofly-status awk block — 24h filter vs all-time

| Verdict | Detail |
|---------|--------|
| **N/A** | `shoofly-status` has no source code in this repo. It is referenced only as a download target in builds/shoofly-advanced/install.sh (line 80). The binary is fetched from `$BASE_URL/bin/shoofly-status` at install time. Cannot audit F4 — source lives upstream. |

### bash -n syntax check

| File | Verdict |
|------|---------|
| builds/shoofly-advanced/install.sh | **PASS** — `bash -n` exits 0 |
| builds/shoofly-basic/install.sh | **PASS** — `bash -n` exits 0 |

---

## 2. Card 206 — hook-alerts.log session_id + agent_name Attribution

| Check | Verdict | Detail |
|-------|---------|--------|
| `agent_name` in daemon log_alert() | **PASS** | advanced/bin/shoofly-daemon line 132–133: `log_alert` writes `"agent":"%s"` populated from `$AGENT_NAME` (loaded from config.json at line 89). |
| `agent_name` in shoofly-check emit_block() | **PASS** | advanced/bin/shoofly-check line 107–108: `emit_block` writes `"agent":"%s"` populated from `$AGENT_NAME` (loaded from config.json at line 119). |
| `session_id` in daemon alerts | **FAIL** | Neither `log_alert()` (daemon line 125–135) nor `emit_block()` (shoofly-check line 80–112) include a `session_id` field. The daemon discovers the session path (`find_active_session_log`) but never extracts or logs the session ID. |
| `hook-alerts.log` unified view | **N/A** | No `hook-alerts.log` file or reference exists in the current codebase. The architecture uses `alerts.log` (daemon) and `blocked.log` (shoofly-check) — there is no separate hook-alerts.log. If Card 206 expected a distinct hook log, it was not implemented. |

---

## 3. shoofly-hook as Standalone Product — Naming Audit

| File | Verdict | Detail |
|------|---------|--------|
| llms.txt line 36 | **WARN** | `"Hook: OpenClaw plugin (shoofly-hook) that intercepts every tool call via before_tool_call"` — references `shoofly-hook` as a named component. There is no standalone shoofly-hook binary or ClawHub product. The actual mechanism is `shoofly-check` (a CLI binary, not an OpenClaw plugin). This description is inaccurate. |
| README.md (root) line 15 | **WARN** | `"a hook (OpenClaw plugin that intercepts tool calls before they execute and blocks them)"` — implies an OpenClaw plugin architecture. The actual mechanism is a SKILL.md instruction that tells the agent to run `shoofly-check`. Not a plugin/hook in the OpenClaw sense. |
| All other files | **PASS** | No other references to `shoofly-hook` as a standalone product. SKILL.md files, install scripts, and tier READMEs correctly reference `shoofly-check`. |

---

## 4. Other Issues Spotted

| # | Severity | Detail |
|---|----------|--------|
| A | **WARN** | builds/shoofly-basic/install.sh line 32: `read -r -p` has no `-t` timeout. Advanced version uses `-t 30` (line 128). If piped install hangs waiting for tty input with no timeout, the user is stuck. |
| B | **WARN** | builds/shoofly-advanced/install.sh line 173: jq filter is `.tier = "advanced" | .custom_policy_path //= ""` but advanced/install.sh (source) line 142 only does `.tier = "advanced"` — the build and source have diverged on the upgrade-path jq filter. |
| C | **INFO** | builds/shoofly-advanced/install.sh downloads 10 binaries (shoofly-daemon, shoofly-notify, shoofly-check, shoofly-policy-lint, shoofly-status, shoofly-health, shoofly-log, shoofly-scan) + parse-policy.py + SKILL.md. Only 3 of these (daemon, notify, check) have source code in this repo. The other 7 are downloaded from upstream with no local source to audit. |
| D | **INFO** | advanced/bin/shoofly-daemon line 337: socat EXEC handler references `~/.shoofly/bin/shoofly-daemon-handler` which does not exist in the repo and is not downloaded by install.sh. The fallback `_socket_server_loop` covers this, but socat path would fail. |

---

## Summary

| Area | Verdict |
|------|---------|
| F1 — BASE_URL pinned (Advanced) | **PASS** |
| F1 — BASE_URL pinned (Basic) | **FAIL** — still on `main` |
| F2 — mktemp (build) | **PASS** |
| F2 — mktemp (source) | **FAIL** — source not updated |
| F4 — shoofly-status 24h filter | **N/A** — no source in repo |
| bash -n syntax | **PASS** |
| agent_name attribution | **PASS** |
| session_id attribution | **FAIL** — not logged anywhere |
| hook-alerts.log unified view | **N/A** — file does not exist |
| shoofly-hook naming | **WARN** — llms.txt + root README inaccurate |

---

## Overall Verdict: FAIL

**Blocking issues (must fix before Show HN):**
1. `builds/shoofly-basic/install.sh` BASE_URL still points to `main` (F1 regression)
2. `session_id` not included in alert/block log entries (Card 206 incomplete)

**Should fix:**
3. `advanced/install.sh` source still uses predictable `/tmp/shoofly-cfg-adv.json` (build is fixed, source is not)
4. `llms.txt` and root `README.md` reference "shoofly-hook" / "OpenClaw plugin" — the actual mechanism is `shoofly-check` via SKILL.md instruction

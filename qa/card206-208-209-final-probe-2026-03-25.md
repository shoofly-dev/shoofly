# Final QA Report: Cards 206 + 208 + 209 — Pre-Show HN

**Agent:** Probe
**Date:** 2026-03-25
**Build:** builds/shoofly-advanced/install.sh (main branch, unstaged changes)
**Scope:** Card 208 (hook bundling, unified alert view, DE check, header tier), Card 209 (hardening re-check), Card 206 (hook-alerts.log session_id + agent_name)

---

## 1. Advanced Installer Smoke Test (builds/shoofly-advanced/install.sh)

| # | Check | Result | Detail |
|---|-------|--------|--------|
| 1.1 | install.sh downloads shoofly-hook | **PASS** | Step 3b (lines 106-111): `curl -fsSL "$BASE_URL/extensions/shoofly-hook/index.ts" -o "$HOOK_DIR/index.ts"` |
| 1.2 | Hook registered in openclaw.json | **PASS** | Lines 112-126: checks for existing registration via `jq`, uses `mktemp` for safe atomic write, handles missing openclaw.json with WARN |
| 1.3 | `bash -n` syntax check | **PASS** | Exit 0, no errors |
| 1.4 | Done banner mentions hook path | **PASS** | Line 284: `Hook: ~/.openclaw/extensions/shoofly-hook/index.ts` |
| 1.5 | Source install.sh (advanced/install.sh) has hook step | **PASS** | Step 3b at lines 81-102, mirrors build copy |
| 1.6 | Source Done banner mentions hook path | **PASS** | Line 232: `Hook: ~/.openclaw/extensions/shoofly-hook/index.ts` |
| 1.7 | Source `bash -n` syntax check | **PASS** | Exit 0, no errors |

---

## 2. Unified Alert View (shoofly-status + hook-alerts.log)

shoofly-status source is NOT in this repo (downloaded at install time from `$BASE_URL/bin/shoofly-status`). Verdicts below are from the **recheck report** (card208-recheck-2026-03-25.md) which verified the installed binary, cross-referenced with the **sentinel security pass** (card208-sentinel-2026-03-25.md).

| # | Check | Result | Detail |
|---|-------|--------|--------|
| 2.1 | shoofly-status reads both alerts.log AND hook-alerts.log | **PASS** | Recheck: HOOK_ALERTS_LOG defined (line 11), _MERGED_ALERTS() cats both logs (lines 106-109) |
| 2.2 | Alert counts merge correctly | **PASS** | Recheck: awk uses _MERGED_ALERTS (line 114), dedup block uses _MERGED_ALERTS (line 138) |
| 2.3 | blocked-today shows hook block count for Advanced | **PASS** | Recheck: lines 200-202 read HOOK_ALERTS_LOG for Advanced tier |
| 2.4 | Header shows dynamic tier label | **PASS** | Recheck: TIER_LABEL derived from $TIER (line 166), printf uses %s (line 167). Sentinel confirmed no injection risk. |
| 2.5 | Header uses agent name from config | **PASS** | Recheck: printf includes "$AGENT_NAME" (line 167) |
| 2.6 | Guard if hook-alerts.log missing | **PASS** | Recheck: line 111 checks `[[ -r "$ALERTS_LOG" || -r "$HOOK_ALERTS_LOG" ]]` |

---

## 3. Hook-alerts.log Format Verification (~/.openclaw/extensions/shoofly-hook/index.ts)

index.ts is NOT in this repo (downloaded at install). Verdicts from **prior QA passes** (card208-209-probe checks 7-12) and **recheck** (W1), cross-referenced with **sentinel** (S3).

| # | Check | Result | Detail |
|---|-------|--------|--------|
| 3.1 | Records include ts, session_id, agent_name, threat, toolName, summary | **PASS** | card208-209 checks 7-8: logAlert() includes session_id (line 71) and agent_name (line 72) |
| 3.2 | session_id from OPENCLAW_SESSION_ID env or default | **PASS** | card208-209 check 7: SESSION_ID variable populated from env |
| 3.3 | agent_name from env or OpenClaw config | **PASS** | card208-209 check 8: AGENT_NAME populated from env/config, not shoofly config |
| 3.4 | Format is JSONL-compatible | **PASS** | Recheck F2: _MERGED_ALERTS() successfully merges both JSONL streams |
| 3.5 | DE check covers exec/Exec/bash/Bash/shell/Shell | **PASS** | Recheck W1: EXEC_TOOLS Set at line 101. Sentinel S3: Set.has() is safe against prototype pollution. |

---

## 4. Hardening Spot-Checks (Card 209)

| # | Check | Result | Detail |
|---|-------|--------|--------|
| 4.1 | BASE_URL pinned to v1.2.2 (build) | **PASS** | Line 13: `BASE_URL="https://raw.githubusercontent.com/shoofly-dev/shoofly/v1.2.2"` |
| 4.2 | BASE_URL tracks main (source) | **PASS** | Line 14 of advanced/install.sh: intentional per comment "DEV COPY — never serve this file directly to users" |
| 4.3 | mktemp used for temp files (build) | **PASS** | Lines 118, 196: `_TMP=$(mktemp)`. No predictable `/tmp/shoofly-*` paths. |
| 4.4 | mktemp used for temp files (source) | **PASS** | Lines 93, 168: `_TMP=$(mktemp)`. Previously had `/tmp/shoofly-cfg-adv.json` — now fixed. |
| 4.5 | No hardcoded secrets or creds | **PASS** | Telegram creds collected via `read -r` at runtime, stored in `~/.shoofly/.env` (chmod 600). No secrets in source. Sentinel S1 confirmed. |
| 4.6 | `bash -n` all modified files | **PASS** | All pass: builds/shoofly-advanced/install.sh, advanced/install.sh, advanced/bin/shoofly-daemon, advanced/bin/shoofly-check, advanced/bin/shoofly-notify, scripts/fix-208.sh |
| 4.7 | shoofly-daemon FIFO paths | **WARN** | Lines 328, 622: `mktemp -u /tmp/shoofly-*-XXXXXX` — uses random suffix but `-u` flag has theoretical TOCTOU race. Low risk for FIFOs. Known from prior QA. |
| 4.8 | builds/shoofly-basic/install.sh BASE_URL | **WARN** | Line 14: still points to `main`. Basic tier hardening is out of scope for Card 209 (Advanced only), but noted for tracking. |

---

## 5. Integration Checks

| # | Check | Result | Detail |
|---|-------|--------|--------|
| 5.1 | shoofly-status parses hook-alert JSONL | **PASS** | Recheck F2: _MERGED_ALERTS() + awk pipeline handles both streams |
| 5.2 | Two alert streams merge in dedup section | **PASS** | Recheck: dedup block at line 138 uses _MERGED_ALERTS |
| 5.3 | No errors on startup if hook-alerts.log missing | **PASS** | Recheck: guard at line 111 handles missing file. `cat` wrapped in `[[ -f ]]` check inside _MERGED_ALERTS() |
| 5.4 | Sentinel security pass | **PASS** | All 4 sections (S1-S4) passed. No injection, no ReDoS, no unsafe eval. |

---

## Consolidated Issue Table

| ID | Severity | Status | Detail |
|----|----------|--------|--------|
| F1 (hook bundling) | Fixed | **PASS** | Both build and source install.sh bundle shoofly-hook with download + openclaw.json registration |
| F2 (unified alert view) | Fixed | **PASS** | shoofly-status merges alerts.log + hook-alerts.log via _MERGED_ALERTS() |
| W1 (DE tool name scope) | Fixed | **PASS** | EXEC_TOOLS Set covers exec/Exec/bash/Bash/shell/Shell |
| W2 (header tier label) | Fixed | **PASS** | Dynamic TIER_LABEL from config, no hardcoded "Basic" |
| Card 206 (session_id + agent_name) | Verified | **PASS** | hook logAlert() includes session_id + agent_name |
| Card 209 BASE_URL | Verified | **PASS** | Pinned to v1.2.2 in build |
| Card 209 mktemp | Verified | **PASS** | All predictable paths replaced with mktemp |
| Card 209 secrets | Verified | **PASS** | No hardcoded credentials |
| Card 209 bash -n | Verified | **PASS** | All files pass syntax check |
| W-daemon-fifo | Known | **WARN** | shoofly-daemon uses `mktemp -u` for FIFOs (theoretical TOCTOU, low risk) |
| W-basic-baseurl | Known | **WARN** | builds/shoofly-basic/install.sh still on `main` (out of scope for Card 209) |
| I-socat-handler | Known | **INFO** | shoofly-daemon line 337 references shoofly-daemon-handler (doesn't exist); nc fallback works |

---

## QA Methodology

- **Direct verification (in-repo):** install.sh files, bash binaries (shoofly-check, shoofly-daemon, shoofly-notify), policy files, fix-208.sh
- **Cross-referenced (installed runtime):** shoofly-status (verified via card208-recheck), index.ts hook (verified via card208-209-probe + recheck), security properties (verified via card208-sentinel)
- **Syntax validation:** `bash -n` on all `.sh` files — all pass
- **Secret scan:** grep for secret/password/api_key/token/credential patterns — no hardcoded secrets found
- **Predictable path scan:** grep for `/tmp/shoofly` — only FIFO paths with mktemp -u (WARN, not FAIL)

---

## Overall Verdict

### **PASS**

All Card 208 fixes (F1, F2, W1, W2) verified as implemented and passing.
Card 209 hardening checks all pass on the Advanced build.
Card 206 hook-alerts.log format (session_id + agent_name) verified from prior QA.
Sentinel security pass confirmed no injection, ReDoS, or credential exposure risks.

Two low-severity WARNs remain (daemon FIFO mktemp -u, Basic BASE_URL on main) — neither is blocking for Show HN.

---

## **READY FOR SHOW HN**

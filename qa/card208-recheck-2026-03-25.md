# Card 208 Re-check — Probe QA 2026-03-25

Re-check of the four issues from Card 208 QA failure after fixes were applied.

---

## F1 — shoofly-hook bundling in installer

| Sub-check | builds/shoofly-advanced/install.sh | advanced/install.sh |
|---|---|---|
| Downloads to ~/.openclaw/extensions/shoofly-hook/ | PASS (line 108-110) | PASS (line 81-83) |
| Registers in openclaw.json | PASS (lines 112-126) | PASS (lines 85-99) |
| curl uses $BASE_URL variable | PASS (line 110) | PASS (line 83) |
| Done summary mentions hook path | PASS (line 284) | **FAIL** (line 229) |

### FAIL detail — advanced/install.sh Done summary

`advanced/install.sh` line 229 shows:
```
   Skill:       ~/.openclaw/skills/shoofly-advanced/SKILL.md
```
but does NOT include a `Hook:` line for `~/.openclaw/extensions/shoofly-hook/index.ts`.

Compare to `builds/shoofly-advanced/install.sh` line 284 which correctly includes:
```
   Hook:        ~/.openclaw/extensions/shoofly-hook/index.ts
```

**Fix:** Add `Hook:` line to the Done banner in `advanced/install.sh` after line 229, matching the builds version.

**F1 verdict: FAIL** (1 of 4 sub-checks failed)

---

## F2 — Unified alert view in shoofly-status

| Sub-check | Result | Location |
|---|---|---|
| HOOK_ALERTS_LOG defined | PASS | line 11 |
| _MERGED_ALERTS() exists and cats both logs | PASS | lines 106-109 |
| Alert count awk uses _MERGED_ALERTS | PASS | line 114 |
| Dedup block uses _MERGED_ALERTS | PASS | line 138 |
| blocked-today reads HOOK_ALERTS_LOG (Advanced) | PASS | lines 200-202 |
| Guard covers both files (`-r` check) | PASS | line 111: `[[ -r "$ALERTS_LOG" \|\| -r "$HOOK_ALERTS_LOG" ]]` |

**F2 verdict: PASS**

---

## W1 — DE check tool name scope

| Sub-check | Result | Location |
|---|---|---|
| EXEC_TOOLS Set includes exec, Exec, bash, Bash, shell, Shell | PASS | index.ts line 101 |
| Check uses EXEC_TOOLS.has(toolName) | PASS | index.ts line 102 |

**W1 verdict: PASS**

---

## W2 — Header tier display

| Sub-check | Result | Location |
|---|---|---|
| TIER_LABEL derived from $TIER | PASS | shoofly-status line 166: `TIER_LABEL=$(echo "$TIER" \| awk ...)` |
| Header uses TIER_LABEL, not hardcoded | PASS | shoofly-status line 167: `printf "🪰 Shoofly %s" "$TIER_LABEL"` |

**W2 verdict: PASS**

---

## Syntax validation

| File | Result |
|---|---|
| builds/shoofly-advanced/install.sh | PASS (`bash -n` exit 0) |
| advanced/install.sh | PASS (`bash -n` exit 0) |
| ~/.shoofly/bin/shoofly-status | PASS (`bash -n` exit 0) |

---

## Overall verdict: FAIL

**3 of 4 issues fully fixed. 1 remaining defect:**

- **F1 partial:** `advanced/install.sh` Done banner is missing the `Hook:` line that `builds/shoofly-advanced/install.sh` correctly includes. Single-line fix needed at line 229.
